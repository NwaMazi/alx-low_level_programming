#include <stdio.h>
#include "main.h"

/**
 * _abs - Prints the absolute value of a number
 * @n: Number to be checked
 * Return: n or -n
 */

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
