#include "main.h"

/**
 * _abs - this program computes the absolute value of an integer
 * @n: reference
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
