#include "main.h"

/**
 * char *_strcat - this program concatinates but appends two strings.
 * @dest: destination
 * @src: origin
 * Return: destination
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
